#pragma once

// #include <discord.h>

// doesnt work, pls fix <3

namespace Lilith {
	/*struct DiscordState {
		discord::User currentUser;

		std::unique_ptr<discord::Core> core;
	}; */

	class DiscordRPC
	{
	public:
		DiscordRPC();
		~DiscordRPC();
	private:
	};
}
